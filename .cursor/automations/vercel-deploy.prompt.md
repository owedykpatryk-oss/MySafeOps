MySafeOps deploy assist.

Use @vercel MCP to inspect latest deployment (preview or production).

If build failed or runtime error:
1. Read deployment/build logs.
2. Branch fix/deploy-<topic> from main.
3. Fix env/build/config minimally.
4. Open PR with deploy verification steps.

Follow AGENTS.md. No secret values in commits.
